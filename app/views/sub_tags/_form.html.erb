<%= form_for(sub_tag) do |f| %>
  <% if sub_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_tag.errors.count, "error") %> prohibited this sub_tag from being saved:</h2>

      <ul>
      <% sub_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
