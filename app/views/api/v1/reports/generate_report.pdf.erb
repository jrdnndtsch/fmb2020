<h1>Your Catalogue of Books</h1>


<% items.each do |item| %>
	<div class="product">
		<div>
			<h2><%= item['product_title'] %></h2>	
			<%= image_tag item['image']%>
			<!-- SERIES INFO -->
			<% if item['series_name'].present? %>
				<p><strong>Series Name:</strong> <%= item['series_name']%></p>
			<% end %>
			<% if item['series_number'].present? %>
				<p><strong>Series Number:</strong> <%= item['series_number']%></p>
			<% end %>
	
			<!-- CONTRIBUTOR INFO -->
			<% Array(item['authors']).each do |author| %>
				<p>author: <%= author%></p>
			<% end %>
			<% Array(item['illustrators']).each do |illustrator| %>
				<p>illustrator: <%= illustrator%></p>
			<% end %>
			<% Array(item['translators']).each do |translator| %>
				<p>translator: <%= translator%></p>
			<% end %>
			<% Array(item['editors']).each do |editor| %>
				<p>editor: <%= editor%></p>
			<% end %>
		</div>
		<div>
			<!-- RIGHTS HOLDER INFO -->
			<p class="title">Rights Holder Info</p>
			<% if item['rights_holder'].present? %>
				<p><strong>Rights Holder (Company):</strong> <%= item['rights_holder']%></p>
			<% end %>
			<% if item['rights_holder_info'].present? %>
				<% Array(item['rights_holder_info']).each do |key, val| %>
					<p><strong><%= key.to_s%>:</strong><%= val %></p>
				<% end %>
			<% end %>
			<p class="title">Additional Info</p>
			<% if item['number_of_pages'].present? %>
				<p><strong># of pages:</strong> <%= item['number_of_pages']%></p>
			<% end %>
			<% if item['age_range']%>
				<p><strong>age range:</strong> <%= item['age_range']%></p>
			<% end %>
			<% if item['age_range_other']%>
				<p><strong>age range:</strong> <%= item['age_range_other']%></p>
			<% end %>
			<% if item['original_language'].present? %>
				<p><strong>Original Language of Publication:</strong> <%= item['original_language']%></p>
			<% end %>
			<% if item['materials_available'].present? %>
				<p><strong>Materials Available:</strong> <%= item['materials_available']%></p>
			<% end %>
			<% if item['rights_available'].present? %>
				<p><strong>Rights Available:</strong> <%= item['rights_available']%></p>
			<% end %>
			<% if item['url'].present? %>
				<a href="<%= ENV['shopify_url'] %><%= item['url']%>"><strong>FBM2020 link:</strong><%= ENV['shopify_url'] %><%= item['url']%></a>
			<% end %>
		</div>

		
		<p class="desc"><%= item['product_description']%></p>
	</div>
	<hr>
<% end %>


<style>
	img {
		max-width: 200px;
		max-height: 400px;
	}
	p, a {
		font-size: 12px;
	}
	a {
		color:black;
	}
	.product > div {
		float: left;
		width: 45%;
	}
	.product >div:last-of-type {
		padding-left: 6%;
	}
	.desc {
		clear: both;
		font-size: 14px;
	}
	p.title {
		border-bottom: 4px solid #ebf5f1;
		font-size: 16px;
		font-weight: bold;
	}
	hr {
		page-break-before: always;
	}

</style>