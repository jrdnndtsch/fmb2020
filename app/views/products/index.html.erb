<h1>Product Page</h1>
<%= link_to 'Products', products_path, method: :post, :remote => true%>
<p>test</p>
<div id="product-list">
	<ul>
	  <% @products.each do |product| %>
	    <li><%= link_to product.title, "https://#{@shop_session.url}/admin/products/#{product.id}", target: "_top" %></li>
	  <% end %>
	</ul>
</div>